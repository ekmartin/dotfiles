syntax on

" Wrap gitcommit file types at the appropriate length
filetype indent plugin on

" Show line numbers
set number

" Set tabs to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

" Fix indenting
set autoindent
set smartindent

